{
  "name": "book",
  "version": "1.0.0",
  "private": true,
  "description": "Book — standalone static page, served as-is.",
  "scripts": {
    "start": "node server.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "express": "^4.19.2"
  }
}
